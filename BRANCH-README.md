# Diff-stream + AY v6 branch

This branch adds synchronized 50 Hz AY playback to the pre-rendered Spectrum
diff-stream player and includes the v6 no-tractor music source.

The integration is deliberately isolated from `main`. No history rewrite or
force update is required.
